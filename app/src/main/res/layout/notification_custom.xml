<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="#00000000"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <!-- App Icon / Logo (Src set in code) -->
    <ImageView
        android:id="@+id/notif_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="16dp" />

    <!-- Balance Text: Use textColorPrimary so it is visible in both Light and Dark notification shades -->
    <TextView
        android:id="@+id/notif_amount"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="--"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="?android:attr/textColorPrimary" />

    <!-- Controls Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Minus Button (Src set in code) -->
        <ImageButton
            android:id="@+id/notif_btn_minus"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:scaleType="centerInside"
            android:padding="8dp" />

        <Space
            android:layout_width="16dp"
            android:layout_height="wrap_content"/>

        <!-- Plus Button (Src set in code) -->
        <ImageButton
            android:id="@+id/notif_btn_plus"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:scaleType="centerInside"
            android:padding="8dp" />
            
    </LinearLayout>

</LinearLayout>